[package]
name = "frt-dpi-xsim"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[target.'cfg(target_os = "linux")'.dependencies]
frt-dpi = { workspace = true }
libc = { workspace = true }
