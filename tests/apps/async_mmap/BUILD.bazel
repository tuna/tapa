"""The async_mmap test for TAPA."""

load("//bazel:test_rules.bzl", "tapa_app_test")

exports_files(
    ["async_mmap.cpp"],
    visibility = [
        # `//tapa-core:tapacc_conformance_test` analyzes this kernel with the
        # real tapacc to check its output against the tapa-ir schema.
        "//tapa-core:__pkg__",
        "//tests/apps:__pkg__",
        "//tests/functional:__subpackages__",
    ],
)

tapa_app_test(
    name = "async_mmap",
    host_srcs = glob([
        "*.cpp",
        "*.h",
    ]),
    hw_test_args = ["1000"],
    macos_sim_args = ["1000"],
    top_name = "AsyncTop",
)
